spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yoga_app_db
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: root@root.com
      password: root

server:
  error:
    whitelabel:
      enabled: false
